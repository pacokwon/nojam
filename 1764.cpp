#include <algorithm>
#include <stdio.h>
#include <map>
#include <vector>

using namespace std;

int main(void) {
    map<string, int> mp;
    int n, m;
    scanf("%d%d", &n, &m);

    for (int i = 0; i < n; i++) {
        
    }

    return 0;
}